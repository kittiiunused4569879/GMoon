# GMoon
# download olemma for windows -------------# https://ollama.com/
# verify download then run commang in cmd # ollama run qwen2.5:7b (this is more 7 gb)or  #ollama run qwen2.5:1.5b  (this is 1 gb)
# create main.py write code in that
# open vs code  new terminal write #pip install requests faster-whisper pygame sounddevice numpy, pip install piper-tts sounddevice numpy,
# install for speed voive #pip install pyttsx3(voice slow dont use), #pip install edge-tts playsound==1.2.2
#pip install googlesearch-python, pip install openwakeword(for hay gmmon)
pip install speechrecognition requests googlesearch-python opencv-python pyaudio
ollama pull qwen2.5:1.5b
ollama pull llava # monitor object moving,screenshot. gig file 4.1 gb instead we can use minicpm-v 
